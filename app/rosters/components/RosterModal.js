import { usePageContext } from "@/app/context/pageContext";
import React from "react";


export default function RosterModal(rosterId){
  const { isMobile, rosters } = usePageContext();

  return (
    <div>
      Test
    </div>
  );
};
