import { usePageContext } from "@/app/context/pageContext";

export default function VacationsMobile({month, year}){
    const {doctors, vacations} = usePageContext();
    
    return (
        <table>

        </table>
    )
}